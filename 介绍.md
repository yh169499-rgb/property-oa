# 本轮人员工作台

- 旧档案导入先预览后确认：手机号优先匹配；否则仅接受唯一姓名；同名进入冲突列表。
- 确认只更新勾选字段，在事务内写入，并使用规范化 JSON 的 SHA-256 `import_key` 防重复。
- Task 9 人员报告支持日期和小区筛选，包含人员、日期、小区、接单/完工双口径、工单、考勤和生成时间，并支持复制、打印、Word 导出。
- 设置 `DB_PATH` 后运行 `node scripts/verify-workforce-migration.js`，脚本仅迁移副本并打印五项核验结果。
- Task 7 签到/补卡前端入口本轮明确不启用。
